# 数据库与存储设计

## 当前阶段

- **会话与 UI 状态**：运行于浏览器内存（Vue 响应式状态）；刷新后丢失。
- **项目与媒体索引**：后续可落地为 **IndexedDB** 或本地文件侧车 JSON；字段与版本在物理层 `spec.md` 演进时同步。

## 广义存储原则

- 大文件（视频/音频素材）**不**存入关系型数据库；仅存路径、时长、缩略图引用等元数据。
- 合成结果导出路径与临时缓存目录由运维文档说明。
