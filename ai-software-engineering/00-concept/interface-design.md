# 接口设计（程序间）

本文件仅描述 **程序间** 接口形态，**不包含** GUI 操作说明。

## 当前阶段

- **对内**：Vue 应用以组件与组合式函数调用为主，无对外 REST 硬性要求。
- **扩展**：若接入 **MCP** 或其它服务，在此文件增补 **传输方式、认证、错误语义**；与 `02-physical/media-studio-vue/spec.md` 对齐。

## GUI

窗口、菜单、快捷键与客户区交互见 `product-design.md` 与 `01-logic/` 中 UI 相关详细设计。
