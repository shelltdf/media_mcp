/**
 * 传给 `showOpenFilePicker` / `showSaveFilePicker` 的 `id`，
 * 便于浏览器分别记忆「导入」与「导出/另存为」的上次目录（支持时）。
 */
export const FILE_PICKER_ID_IMPORT = "media-studio-import";
export const FILE_PICKER_ID_EXPORT = "media-studio-export";
